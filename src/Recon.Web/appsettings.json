{
  "ConnectionStrings": {
    "Recon": ""
  },
  "Recon": {
    "// Scheduler": "Set Enabled to false to run the portal without the background scheduler.",
    "Scheduler": {
      "Enabled": true,
      "PollSeconds": 60
    },
    "// Acquisition": "Where acquired files are kept. Originals are never stored in the database.",
    "StorageRoot": "/var/recon/files"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
